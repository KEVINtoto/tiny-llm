// WARNING: Under review - generated by LLM.

//! Week 4, Day 7: evaluate one run from declared observable facts.

use crate::r#loop::AgentRun;
use crate::protocol::AgentError;
use crate::receipts::ReceiptStore;
use crate::workspace::Workspace;

/// A fallible, immutable receipt view for evaluation (including persisted logs).
pub trait ReceiptLookup {
    fn snapshot(&self) -> Result<Box<dyn ReceiptLookup>, AgentError>;
    fn get_receipt(
        &self,
        tool_call_id: &str,
    ) -> Result<Option<crate::receipts::EffectReceipt>, AgentError>;
}

impl ReceiptLookup for ReceiptStore {
    fn snapshot(&self) -> Result<Box<dyn ReceiptLookup>, AgentError> {
        // TODO: reopen persisted logs to detect tampering, or snapshot memory.
        todo!()
    }
    fn get_receipt(
        &self,
        tool_call_id: &str,
    ) -> Result<Option<crate::receipts::EffectReceipt>, AgentError> {
        Ok(self.get(tool_call_id).cloned())
    }
}

/// One exact UTF-8 file fact expected after the run.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct FileExpectation {
    pub path: String,
    pub content: String,
}

impl FileExpectation {
    pub fn new(path: String, content: String) -> Result<Self, AgentError> {
        let expectation = Self { path, content };
        expectation.post_init()?;
        Ok(expectation)
    }

    /// Python's ``__post_init__``.
    pub fn post_init(&self) -> Result<(), AgentError> {
        todo!()
    }
}

/// One required substring in an observable tool result.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct ResultExpectation {
    pub tool: String,
    pub contains: String,
}

impl ResultExpectation {
    pub fn new(tool: String, contains: String) -> Result<Self, AgentError> {
        let expectation = Self { tool, contains };
        expectation.post_init()?;
        Ok(expectation)
    }

    /// Python's ``__post_init__``.
    pub fn post_init(&self) -> Result<(), AgentError> {
        todo!()
    }
}

/// Declared facts for one named durable effect receipt.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct ReceiptExpectation {
    pub tool_call_id: String,
    pub tool: String,
    pub exit_state: String,
    pub result_contains: String,
    /// Python default: ``()``.
    pub changed_artifacts: Vec<String>,
}

impl ReceiptExpectation {
    pub fn new(
        tool_call_id: String,
        tool: String,
        exit_state: String,
        result_contains: String,
        changed_artifacts: Vec<String>,
    ) -> Result<Self, AgentError> {
        let expectation = Self {
            tool_call_id,
            tool,
            exit_state,
            result_contains,
            changed_artifacts,
        };
        expectation.post_init()?;
        Ok(expectation)
    }

    /// Python's ``__post_init__``.
    pub fn post_init(&self) -> Result<(), AgentError> {
        todo!()
    }
}

/// The observable facts one deterministic course case requires.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct EvaluationCase {
    pub final_contains: String,
    /// Python default: ``()``.
    pub files: Vec<FileExpectation>,
    /// Python default: ``()``.
    pub results: Vec<ResultExpectation>,
    /// Python default: ``()``.
    pub receipts: Vec<ReceiptExpectation>,
}

impl EvaluationCase {
    pub fn new(
        final_contains: String,
        files: Vec<FileExpectation>,
        results: Vec<ResultExpectation>,
        receipts: Vec<ReceiptExpectation>,
    ) -> Result<Self, AgentError> {
        let case = Self {
            final_contains,
            files,
            results,
            receipts,
        };
        case.post_init()?;
        Ok(case)
    }

    /// Python's ``__post_init__``.
    pub fn post_init(&self) -> Result<(), AgentError> {
        todo!()
    }
}

/// One named PASS/FAIL result backed by an observable fact.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct EvaluationCheck {
    pub name: String,
    pub passed: bool,
    pub detail: String,
}

/// Stable ordered checks for one evaluation case.
#[derive(Debug, Clone, PartialEq, Eq, Hash)]
pub struct EvaluationReport {
    pub checks: Vec<EvaluationCheck>,
}

impl EvaluationReport {
    pub fn passed(&self) -> bool {
        todo!()
    }

    pub fn render(&self) -> String {
        todo!()
    }
}

/// Judge one run only from the observable facts declared by `case`.
pub fn evaluate_run(
    run: &AgentRun,
    workspace: &Workspace,
    receipts: &dyn ReceiptLookup,
    case: &EvaluationCase,
) -> EvaluationReport {
    todo!()
}
